

  # 设置该服务注册中心的 hostname
eureka:
  instance:
    hostname: eureka8761
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka8762:8762/eureka/
server:
  port: 8761
