server:
  port: 8762

  # 设置该服务注册中心的 hostname
eureka:
  instance:
    hostname: eureka8762
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://eureka8761:8761/eureka/
